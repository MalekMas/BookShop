<!DOCTYPE html>
<html lang="en">

<head>
    <title>
        <%= title %>
    </title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%- include('../partials/includes') %>

        <style>
            body {
                margin: 0;
                padding: 0;
                color: #f1f1f1;
            }

            .jumbotron {
                background: linear-gradient(115deg, #1A1A27, #0a0a0e);
                color: #e0e0e0;
                padding: 3rem 1.51rem;
                box-shadow: 0 6px 20px rgba(0, 0, 0, 0.7);
                text-align: center;
                margin-bottom: 0;
                padding-bottom: 3rem;
            }

            .jumbotron h1 {
                font-weight: 600;
                font-size: 3rem;
                color: #ffffff;
                letter-spacing: 1px;
            }

            .jumbotron p {
                font-weight: 300;
                font-size: 1.2rem;
                color: #cccccc;
            }

            a {
                color: #90caf9;
            }

            a:hover {
                color: #bbdefb;
            }
        </style>
</head>

<body>
    <div class="jumbotron">
        <h1>Malek Book Shop</h1>
        <p>Explore a curated collection of great reads</p>
    </div>
    <%- include('nav') %>